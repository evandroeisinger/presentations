// Funções
var teste = function( valor ) {
	// Função que mostra o valor do paramentro passado.
	console.log( "Este é o valor " + valor );
};
// Funções
var outro = function( valor ) {
	// Função que mostra o valor do paramentro passado.
	console.log( "Este é o valor " + valor );
};
// Funções
var novo = function( valor ) {
	// Função que mostra o valor do paramentro passado.
	if ( valor ) {
		console.log( "Este é o valor " + valor );
	}
};